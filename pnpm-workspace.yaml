packages:
  - "apps/*"
  - "packages/*"

# Native/binary deps that legitimately need install scripts.
allowBuilds:
  # jsPDF transitive dep; its postinstall is only a funding notice, nothing to build.
  core-js: false
  esbuild: true
  # Downloads the Workers runtime binary wrangler uses for local preview/build.
  workerd: true

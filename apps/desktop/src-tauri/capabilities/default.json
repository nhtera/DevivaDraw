{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal surface for the main window: core APIs, native dialogs, window-state persistence. Deliberately NO fs capability — all path-based file I/O goes through allowlist-validated Rust commands (see src/file_io.rs).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "window-state:default"
  ]
}

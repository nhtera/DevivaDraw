{
  "name": "@deviva-draw/collab-server",
  "version": "0.1.0",
  "private": true,
  "description": "Deviva Draw collaboration backend — Cloudflare Worker with Durable Objects rooms and R2 share-link storage",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260804.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.28.0"
  }
}

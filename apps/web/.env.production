# Production build config for the standalone web app (draw.deviva.app).
# The share-link + live-collab backend Worker. Not a secret — it's a public origin.
VITE_SHARE_API_BASE_URL=https://collab-draw.deviva.app

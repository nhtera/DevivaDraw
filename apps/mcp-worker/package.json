{
  "name": "@deviva-draw/mcp-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Deviva Draw remote MCP endpoint — stateless Streamable HTTP on a Cloudflare Worker, no accounts, no server-side scene storage",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@deviva-draw/engine": "workspace:*",
    "@deviva-draw/mcp": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.28.0"
  }
}
